# Override the default simulation store location.
# Default: ~/.terminus/research/sims.db
# TERMINUS_HOME=/path/to/your/research/dir

# Community hub contribution — on by default. Set to 0 to opt out.
# TERMINUS_TELEMETRY=0
